{
  "verbose": true,
  "preset": "ts-jest",
  "testEnvironment": "jest-environment-jsdom",
  "globals": {
    "ts-jest": {
      "tsconfig": "tsconfig.json"
    }
  },
  "moduleNameMapper": {
    "@/(.*)": "<rootDir>/src/$1",
    "\\.(css|less)$": "identity-obj-proxy"
  }
}